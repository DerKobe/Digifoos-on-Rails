- content_for :navbar do
  - if can? :update, @group
    %li
      %a{ href: group_players_path(@group) }
        = fa_icon 'users', text: 'Players'

    %li
      %a{ href: edit_group_path(@group) }
        = fa_icon 'cog', text: 'Edit Group'